"use strict"; // treat all js code as newer version

// alert("Hello"); // we are using Nodejs to run - hence error

console.log(3+9);

let name="Asmita";
let age=32;
let isLoggedIn=false;

// number
// string
// boolean true/false
// null stand alone value 
// undefined if value is not defined. let age;
// symbol => unique
// object
// typeof null => object
// typeof undeinfed => undefined
console.log(typeof age,typeof null,typeof undefined);
